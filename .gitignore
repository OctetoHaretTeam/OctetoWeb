# dependencies
node_modules

# build output
dist
dist-ssr
.output
.nitro
.vinxi
.vercel

# generated
src/routeTree.gen.ts
.tanstack
__unconfig*

# env — never commit real credentials, .env.example is the tracked template
.env
.env.*
!.env.example

# editor / OS
.DS_Store
Thumbs.db
*.local
*.log

# local development database (PGlite) — never committed
.pglite

# local uploads (used when BLOB_READ_WRITE_TOKEN is unset)
public/uploads
!public/logo.webp

# Local database dumps. NEVER committed: they contain real member names and
# the photoConsent / fullNamePublic flags, and members are 14-18 (§8).
# They are the only copy of local content, so keep them — just not in git.
db-backups/
